# Models

This directory contains trained machine learning models.

best_model.joblib
- Final selected Logistic Regression pipeline
- Includes preprocessing transformer
- Selected using validation ROC-AUC
- Evaluated once on the held-out test set